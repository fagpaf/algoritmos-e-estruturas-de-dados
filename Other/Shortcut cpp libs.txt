#include <iostream>     // Para entrada e saída
#include <vector>       // Para o contêiner vector
#include <list>         // Para o contêiner list
#include <map>          // Para o contêiner map
#include <set>          // Para o contêiner set
#include <string>       // Para a classe string
#include <algorithm>    // Para algoritmos padrão como sort e find
#include <iterator>     // Para iteradores
#include <utility>      // Para funções utilitárias como pair
#include <functional>   // Para funções e objetos de função
#include <queue>        // Para contêiner queue
#include <stack>        // Para contêiner stack
#include <deque>        // Para contêiner deque
#include <unordered_map>// Para contêiner unordered_map
#include <unordered_set>// Para contêiner unordered_set